[project]
name = "gridworks-wiki"
version = "0.0.0"
description = "The GridWorks design wiki + health-check tests (status stamps, doc size)."
requires-python = ">=3.10"

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
