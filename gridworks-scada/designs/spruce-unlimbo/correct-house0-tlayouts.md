# Correct House0 tlayouts (spoke)

Status: Draft · Pass 0 · Updated 2026-09-10 · Linear: OPS-392

> What this is: the House0 fixture pair generated by a sema-native
> tlayouts gen instead of kept by hand. The beech fixture
> `tests/config/gw.house0.layout.json` gained five-v-boss, the cycler
> and the new handles by hand; it fails `sema validate` on pre-existing
> shape (three channels carry InPowerMetering, four components predate
> their words' config shape). `HydronicLayout`'s essential-nodes check
> lists neither five-v-boss nor the cycler; the layout words carry the
> requirement.

## Do this next

1. A translated beech gen in tlayouts (the `*_sema_gen.py` pattern the
   Nolan and sim gens use) emits the House0 pair; `sema validate` passes
   on the output.
2. The scada fixtures `gw.house0.layout.json` and
   `gw.house0.sim.layout.json` come from that gen, never by hand.
3. The relay decommission (`krida-retirement.md` rung 3, item 1)
   hand-edits the two House0 fixtures first (per-relay components, the
   Krida component and `relay-multiplexer` node dropped); that
   hand-edited pair is the contract the gen reproduces byte-for-byte.
   The generator's TODO still points at the deleted `house0-layout.json`.
4. On regen, refresh the vendored closure copy in gwsproto in the same
   wave (the layout-closure mirror maxim). The House0 pair regenerates
   again for the 0-10V shift (`house0-zero-ten-outputs.md`) and the
   fall installs, so the gen, not the fixtures, is the thing to get
   right.

## Open

- Whether `HydronicLayout`'s essential-nodes check should name
  five-v-boss and the cycler, or stay a minimal list with the layout
  words as the requirement.
