# Concern: Sema, and the legacy SCADA style

Design intent from Jessica + foundational reads (`sema/CLAUDE.md`,
`sema/docs/where-meaning-lives-in-gridworks.md`).

## What Sema is (and isn't)

Sema is **boundary infrastructure**: it defines the serialized-JSON contracts
exchanged between independent systems. It governs what crosses a process
boundary as JSON — **not** runtime architecture, database design, or internal
object models. Internal communicator-to-communicator messages within one
proactor are plain Python objects, not Sema.

Core rules (from `sema/CLAUDE.md`):

- Vocabulary words are stable `left.right.dot` names; every type has `TypeName`,
  every versioned type a 3-digit `Version`.
- Serialized JSON field names are **CamelCase**, recursively.
- Primitive constraints live in **named formats** (`positive.int`, `uuid4.str`,
  `utc.seconds`), immutable and unversioned — not inline.
- Enums: `literal` (fixed at `"000"`) or `versioned` (append-only — never
  remove, reorder, or reinterpret).
- Versioning only tightens (`none → string → literal`); historical active
  versions are immutable; semantic changes require a new version.
- Upgrades: `model_dump()` → change only what changed → `model_validate()` into
  the next version; nested types upgrade recursively. Never reconstruct
  field-by-field.

Meaning is **explicit**: any fact that matters for validation/composition is
declared in the Sema schema, never inferred from naming or implementation
(`where-meaning-lives-in-gridworks.md`).

## In the SCADA

The boundary types live in `packages/gridworks-scada-protocol` (`gwsproto`),
published to PyPI. These are the Sema-governed artifacts. Everything else in the
SCADA runtime is *not* bound by Sema rules.

## The legacy problem

> The SCADA is a **legacy implementation of Sema**. The GridWorks style is more
> **horizontal and à la carte**. (Jessica)

The cleanup direction: bring the SCADA's boundary types in line with the current
Sema discipline, and restructure runtime code so concepts can be lifted à la
carte (shallow, composable units) rather than navigated through deep legacy
hierarchies. See [[../principles]].

## Open questions

- Which `gwsproto` types are pre-current-Sema (wrong versioning strategy, inline
  constraints instead of formats, non-CamelCase, etc.)? Needs a pass over
  `packages/gridworks-scada-protocol`.
- What does "legacy implementation of Sema" mean concretely here — generated by
  an older `effortlessapi` toolchain (`docs/sdk-types.rst` hints at this)?
- Where does the SCADA runtime lean on implicit/naming-derived meaning that Sema
  says should be explicit?

## Links

`sema/CLAUDE.md` · `sema/docs/where-meaning-lives-in-gridworks.md` ·
`gridworks-scada/CLAUDE.md` (Boundary protocol vs. runtime) · [[../principles]]
