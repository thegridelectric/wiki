# Changelog

A reverse-chronological log of WHY we made each commit **in the
provisioning code repo** (TBD — provisioning has no code repo yet;
once one is stood up, this header should name it). The matching
git commit holds the WHAT (the diff). Each entry's date and one-line
title mirror the corresponding code-repo commit.

This changelog does NOT track wiki edits — those live in the wiki
repo's git history.

Newest at the top.

---

_No code commits to log yet — provisioning is still in the design
phase. Entries will land here once implementation begins._
